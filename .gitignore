# Personal trip samples (real GPS data) — never commit
/samples/

# Python
.venv/
venv/
__pycache__/
*.py[cod]

# Local DB / data
/data/
*.sqlite
*.sqlite3
*.db

# Env / secrets
.env
*.env

# Playwright MCP transient artifacts (snapshots/screenshots/logs — may contain account data)
.playwright-mcp/
/eucstats-*.png
/members.json

# Local-only encode source for the intro video (committed intro.mp4 is the optimized build)
/web/static/intro_src.mp4

# Playwright/verification screenshots saved to the repo root during dev
/*.jpeg
